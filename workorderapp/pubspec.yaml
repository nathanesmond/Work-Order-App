name: workorderapp
description: "Flutter mobile client for the Work Order Management System."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.0.0
  http: ^1.6.0
  provider: ^6.1.5+1
  go_router: ^14.0.0
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  fl_chart: ^0.66.0
  firebase_core: ^3.0.0
  firebase_messaging: ^15.0.0
  table_calendar: ^3.1.2



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - images/
